fx_version 'cerulean'
game 'gta5'

description 'AIFAZI RP — Whitelist enforcer + bidirectional txAdmin sync bridge'
version     '3.0.0'
author      'aifazi'

server_scripts {
    'config.lua',
    'server.lua'
}
