# Copy to .env and adjust values. Expo auto-loads .env (EXPO_PUBLIC_* are inlined into the bundle).
# All values are optional — defaults point at the production API.

# Base URL of the aifazi.net FastAPI backend (no trailing slash).
EXPO_PUBLIC_API_URL=https://api.aifazi.net